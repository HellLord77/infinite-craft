import {TouchDoubleDirective} from './touch-double.directive';

describe('TouchDoubleDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchDoubleDirective();
    expect(directive).toBeTruthy();
  });
});
